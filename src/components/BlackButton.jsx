import React from 'react';
import './BlackButton.css'

const BlackButton = (props) => {
  return (
    <button className="EstiloBPreto">{props.frase}</button>
  );
};

export default BlackButton;
